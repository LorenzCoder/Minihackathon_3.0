{
  "name": "hackathon-fullstack",
  "version": "1.0.0",
  "description": "Fullstack-Projekt für den Hackathon: React Frontend und Express/Supabase Backend.",
  "main": "backend/server.js",
  "scripts": {
    "start": "npm run frontend:build && node backend/server.js",
    "frontend:build": "npm run build --prefix ./frontend/TeamPanel-Frontend",
    "backend:install": "npm install --prefix ./backend",
    "frontend:install": "npm install --prefix ./frontend/TeamPanel-Frontend",
    "install:all": "npm run backend:install && npm run frontend:install && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "mongoose": "^9.0.0",
    "dotenv": "^16.4.5"
  }
}
